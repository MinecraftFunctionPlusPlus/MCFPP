enum CloneMaskMode{
    masked,
    replace
}

enum CloneOperation{
    force,
    move,
    normal
}

enum Difficulty {
    peaceful,easy,normal,hard
}

enum FillMode{
    destroy,
    hollow,
    replace,
    strict
}

enum GameMode{
    creative,
    spectator,
    survival,
    adventure
}

enum PlaceRotation {
    none, clockwise_90, _180, counterclockwise_90
}

enum PlaceMirror {
    none, left_right, front_back
}

enum SoundType {
    master, music, record, weather, block, hostile, neutral, player, ambient, voice
}
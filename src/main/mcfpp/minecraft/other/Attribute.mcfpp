data Attribute {
    id as string;
    base as double;
    modifiers as list<AttributeModifier>;
}

data AttributeModifier {
    type as AttributeModifierType;
    amount as double;
    id as string;
}

enum AttributeModifierType {
    add_value,
    add_multiplied_base,
    add_multiplied_total
}
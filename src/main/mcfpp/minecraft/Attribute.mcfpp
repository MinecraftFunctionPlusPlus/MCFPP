@From("top.mcfpp.mni.minecraft.AttributeData")
@NoInstance
data Attribute{}

data AttributeModifier {
    AttributeModifierType type;
    string id;
}

enum AttributeModifierType {
    add_value,
    add_multiplied_total,
    add_multiplied_base
}
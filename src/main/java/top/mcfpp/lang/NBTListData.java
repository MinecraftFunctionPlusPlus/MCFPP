package top.mcfpp.lang;

import org.jetbrains.annotations.NotNull;
import top.mcfpp.annotations.MCFPPNative;

public class NBTListData extends MNIData {
    @Override
    void init() {
        addFunction("add", (vars, caller) -> {

        });
    }
}
